FaceControl

It is recommended to be extracted at the root of the C:\ drive.

It should look like this:

C:\FaceControl\client.py

Don't forget to put the IP of the mobile device with Live Link installed and make sure you are on the same network.

python osc module required

pip install python-osc
